# subdomain_enum_project
A simple tool to enumerate subdomains

## How to run
execute 
```sh
nim c -d:ssl main.nim
```
and get main binary
then
```sh
./main
```

Enter a domain and optionally save results to a file